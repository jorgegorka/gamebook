<%= tag.footer class: "mt-10 bg-slate-50 flex justify-center" do %>
  <%= tag.div button_to("Log out", session_path, method: :delete, class: "text-slate-700 px-2 py-1 rounded hover:bg-slate-100") %>
<% end %>
